name: Web

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'binding/web/**'
      - '!binding/web/README.md'
      - 'lib/wasm/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'binding/web/**'
      - '!binding/web/README.md'
      - 'lib/wasm/**'

defaults:
  run:
    working-directory: binding/web

jobs:
  build:
    runs-on: ubuntu-20.04

    strategy:
      matrix:
        python-version: [3.8]
        node-version: [lts/*]

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install system dependencies
        run: |
          sudo apt install curl unzip
<<<<<<< HEAD
=======
          curl --output chromedriver_linux64.zip https://chromedriver.storage.googleapis.com/95.0.4638.54/chromedriver_linux64.zip
          unzip chromedriver_linux64.zip
          sudo mv chromedriver /usr/local/bin
>>>>>>> 5cb0532 (removed google-chrome-version)
          pip install selenium
          pip install webdriver-manager

      - name: Pre-build dependencies
        run: npm install yarn

      - name: Install dependencies
        run: yarn install

      - name: build
        run: |
          yarn build
          cd cobra-web-factory
          yarn
          yarn build
          yarn lint

      - name: Test
        run: python cobra-web-factory/test/selenium_test.py --access_key ${{secrets.PV_VALID_ACCESS_KEY}} --audio_file ../../res/audio/sample.wav
