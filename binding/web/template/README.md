# cobra-web-worker package

## Build

Use `yarn` or `npm` to install the dependencies, and the `build` script to build the package

```console
yarn
yarn build
```

(or)

```console
npm install
npm run build
```